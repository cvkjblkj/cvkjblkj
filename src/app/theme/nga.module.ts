
import { NgModule, ModuleWithProviders } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ReactiveFormsModule, FormsModule } from '@angular/forms';
import { RouterModule } from '@angular/router';
import { ButtonModule, CalendarModule, TabViewModule, ToolbarModule, DialogModule, AccordionModule,GrowlModule, SharedModule, DataTableModule, AutoCompleteModule,SpinnerModule } from 'primeng/primeng';
import { PaginationModule, TabsModule } from 'ng2-bootstrap';
import { Ng2EchartsModule } from './../jasframework/journal-info/ng2-echarts.module'
import {
  BaThemeConfig
} from './theme.config';

import {
  BaThemeConfigProvider
} from './theme.configProvider';

import {
  BaAmChart,
  BaCard,
  BaChartistChart,
  BaCheckbox,
  BaContentTop,
  BaFullCalendar,
  BaMenuItem,
  BaMenu,
  BaMsgCenter,
  BaMultiCheckbox,
  BaPictureUploader,
  NavBar,
  GridpaginationComponent,
  Monitor,
  ossMonitor,

  RuleComponent,
  QueryDataOverloadedComponent,
  EchartsComponent,


} from './components';

import { BaCardBlur } from './components/baCard/baCardBlur.directive';

import {
  BaScrollPosition,
  BaSlimScroll,
  BaThemeRun
} from './directives';

import {
  BaAppPicturePipe,
  BaKameleonPicturePipe,
  BaProfilePicturePipe
} from './pipes';

import {
  BaImageLoaderService,
  BaMenuService,
  BaThemePreloader,
  BaThemeSpinner
} from './services';

import {
  EmailValidator,
  EqualPasswordsValidator
} from './validators';

const NGA_COMPONENTS = [
  BaAmChart,
  BaCard,
  BaChartistChart,
  BaCheckbox,
  BaContentTop,
  BaFullCalendar,
  BaMenuItem,
  BaMenu,
  BaMsgCenter,
  BaMultiCheckbox,
  BaPictureUploader,
  NavBar,
  GridpaginationComponent,
  Monitor,
  ossMonitor,
  RuleComponent,
  QueryDataOverloadedComponent,

  EchartsComponent

];

const NGA_DIRECTIVES = [
  BaScrollPosition,
  BaSlimScroll,
  BaThemeRun,
  BaCardBlur
];

const NGA_PIPES = [
  BaAppPicturePipe,
  BaKameleonPicturePipe,
  BaProfilePicturePipe
];

const NGA_SERVICES = [
  BaImageLoaderService,
  BaThemePreloader,
  BaThemeSpinner,
  BaMenuService
];

const NGA_VALIDATORS = [
  EmailValidator,
  EqualPasswordsValidator
];
const NGA_UICOMPONENTS = [
  ButtonModule,
  CalendarModule,
  TabViewModule,
  ToolbarModule,
  DialogModule,
  AccordionModule,
  SharedModule,
  DataTableModule,
  SpinnerModule,
  GrowlModule,
  PaginationModule.forRoot(),
  TabsModule.forRoot()
]

@NgModule({
  declarations: [
    ...NGA_PIPES,
    ...NGA_DIRECTIVES,
    ...NGA_COMPONENTS
  ],
  imports: [
    CommonModule,
    RouterModule,
    FormsModule,
    ReactiveFormsModule,
    AccordionModule,
    Ng2EchartsModule,
    AutoCompleteModule,
    ...NGA_UICOMPONENTS
  ],
  exports: [
    ...NGA_PIPES,
    ...NGA_DIRECTIVES,
    ...NGA_COMPONENTS
  ]
})
export class NgaModule {
  static forRoot(): ModuleWithProviders {
    return <ModuleWithProviders>{
      ngModule: NgaModule,
      providers: [
        BaThemeConfigProvider,
        BaThemeConfig,
        ...NGA_VALIDATORS,
        ...NGA_SERVICES
      ],
    };
  }
}
